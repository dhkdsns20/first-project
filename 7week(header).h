#ifndef CIRCLE_H
#define CIRCLE_H
// Circle 클래스 선언

class Circle {
private:
	int radius;
public:
	void setRadius(int r);
	int getRadius();
	double getArea();
};
#endif

//CIRCLE_H가 정의 되어 있으므로 선언되지 않는다.
#ifndef CIRCLE_H
#define CIRCLE_H
// Circle 클래스 선언

class Circle {
private:
	int radius;
public:
	void setRadius(int r);
	int getRadius();
	double getArea();
};
#endif